// import { Maybe } from 'monet';

// import { BinaryOperatorRuntime } from './binary-operator-runtime';

// function test(values: Map<string, string>): Maybe<Map<string, Match>> {
//   const { label } = this.ast;

//   return this.children.test(values.filter((_value, key) => key.includes(label)));
// }

// export const like: BinaryOperatorRuntime = (label, getExpression) => {
//     const f = values.filter((_value, key) => key.includes(label));

//     return label.cata(getRightSide, left => Maybe.of(left));
//   }
